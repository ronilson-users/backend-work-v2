# 🔐 SECURITY: Use um secret REAL (mínimo 32 caracteres aleatórios)
JWT_SECRET=9e2f3850036bba7e1e5797f26ae6e6d994383ebfc6002cc82a584f836d3d8a0ef7184c22ef4ffb3e508e5f2f597dc387119a3756bf620f577d5653fa137a6c18

# 🌐 CORS: Defina origens permitidas
CORS_ORIGIN=http://localhost:3000,https://seuapp.com

# 🚀 RATE LIMITING: Proteção contra ataques
RATE_LIMIT_WINDOW_MS=900000        # 15 minutos
RATE_LIMIT_MAX_REQUESTS=100        # 100 requests por 15min

# 📊 DATABASE: Use variáveis separadas (MAIS SEGURO)
DB_USER=work-history
DB_PASSWORD=SUA_SENHA_SEGURA_AQUI  # 🔴 ALTERE ISSO!
DB_HOST=cluster0.srzwvze.mongodb.net
DB_NAME=work-history

# 🔗 MONGODB_URI montada com variáveis seguras
MONGODB_URI=mongodb+srv://${DB_USER}:${DB_PASSWORD}@${DB_HOST}/${DB_NAME}?retryWrites=true&w=majority&appName=Cluster0

# 🧪 TEST ENVIRONMENT
MONGODB_URI_TEST=mongodb+srv://${DB_USER}:${DB_PASSWORD}@${DB_HOST}/test-${DB_NAME}?retryWrites=true&w=majority
NODE_ENV_TEST=test

# ⚙️ APPLICATION
NODE_ENV=development
PORT=3001

# ☁️ AWS (se necessário)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1
AWS_S3_BUCKET=work-history-photos

# 🌐 CDN (opcional)
CLOUDFRONT_DOMAIN=photos.workhistory.com