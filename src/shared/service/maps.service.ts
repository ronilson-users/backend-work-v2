export class MapsService {
  async calculateRoute(locations: WorkLocation[]) {
    // Integrar com Google Maps API ou similar
    // para calcular rotas e tempos de deslocamento
  }

  async getDirections(from: string, to: string) {
    // Obter direções entre locais
  }
}